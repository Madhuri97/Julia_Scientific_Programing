## Question 1: Assume that the Plots package is installed and runs properly. (JuliaBox has the Plots package installed by default). Which line of code loads the Plots package into the current workspace?

